import { ScreenUserPipe } from './screen-user.pipe';

describe('ScreenUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreenUserPipe();
    expect(pipe).toBeTruthy();
  });
});
